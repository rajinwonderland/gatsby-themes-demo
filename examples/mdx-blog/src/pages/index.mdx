# HELLO WORLD!

```jsx
class DEMO extends React.Component {
	render() {
		return (
			<button
				onClick={() => alert('HELLO REACT LIVE!')}
				style={{
					color: 'white',

					borderRadius: 4,
					backgroundColor: '#282a36',
					textTransform: 'uppercase'
				}}>
				Alert
			</button>
		);
	}
}
```
