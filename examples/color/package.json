{
	"name": "color",
	"version": "1.0.0",
	"scripts": {
		"clean": "gatsby clean && rm -rf yarn-error.log",
		"nuke": "yarn run clean && rm -rf node_modules",
		"start": "gatsby develop",
		"develop": "gatsby develop",
		"build": "gatsby build",
		"now-build": "gatsby build --prefix-paths"
	},
	"dependencies": {
		"gatsby": "^2.1.19",
		"gatsby-theme-dracula": "*",
		"react": "^16.8.3",
		"react-dom": "^16.8.3"
	}
}
